export class AddScore {
    constructor(
        public winner?: string,
        breaker?: string,
        stripes?: string,
        solids?: string,
        beatSelf?: string,
        easyPocket?: string,
        ballsLeft?: number
    ) {}
}
